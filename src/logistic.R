# predict using logistic regression

# for each column get:
# total number of unique values
# ratio unique values to nrow
# is float (categorical)
# is integer (categorical)
# was string (categorical)
# distributions... TBD

# need a way to guess at rank (ordering) when string data supplied